-- FREE LIBRARY HUB - MAIN MODULE CODE
local Library = {}
Library.Flags = {}

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local HttpService = game:GetService("HttpService")

-- دعم الصور الخارجية وروابط URL
local function GetImage(url)
    if not url or url == "" then return "" end
    if string.find(url, "rbxassetid://") or string.find(url, "http://www.roblox.com") then
        return url
    elseif string.find(url, "http://") or string.find(url, "https://") then
        if writefile and getcustomasset then
            local filename = "cache_img_" .. string.sub(HttpService:GenerateGUID(false), 1, 8) .. ".png"
            pcall(function()
                writefile(filename, game:HttpGet(url))
            end)
            if isfile and isfile(filename) then
                return getcustomasset(filename)
            end
        end
    end
    return url
end

-- وظيفة جعل العناصر قابلة للسحب (Drag)
local function MakeDraggable(gui)
    local dragging, dragInput, dragStart, startPos
    gui.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = gui.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    gui.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

function Library:CreateWindow(options)
    options = options or {}
    local Title = options.Title or "FREE LIBRARY HUB"
    local AccentColor = options.AccentColor or Color3.fromRGB(160, 32, 240) -- بنفسجي
    local BgColor = options.BgColor or Color3.fromRGB(15, 15, 15) -- أسود

    -- الشاشة الرئيسية
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "FreeLibraryHub"
    ScreenGui.Parent = (gethui and gethui()) or CoreGui

    -- زر التشغيل والإغلاق الدائري (Floating MinSize Button)
    local OpenBtn = Instance.new("ImageButton")
    local OpenUICorner = Instance.new("UICorner")
    local OpenUIStroke = Instance.new("UIStroke")

    OpenBtn.Name = "FloatingMinBtn"
    OpenBtn.Size = UDim2.new(0, 50, 0, 50)
    OpenBtn.Position = UDim2.new(0.1, 0, 0.2, 0)
    OpenBtn.BackgroundColor3 = BgColor
    OpenBtn.Image = GetImage(options.ToggleIcon or "rbxassetid://6031097225")
    OpenBtn.Parent = ScreenGui

    OpenUICorner.CornerRadius = UDim.new(1, 0) -- دائري بالكامل
    OpenUICorner.Parent = OpenBtn

    OpenUIStroke.Color = AccentColor
    OpenUIStroke.Thickness = 2
    OpenUIStroke.Parent = OpenBtn

    MakeDraggable(OpenBtn)

    -- الإطار الرئيسي للواجهة
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 550, 0, 350)
    MainFrame.Position = UDim2.new(0.5, -275, 0.5, -175)
    MainFrame.BackgroundColor3 = BgColor
    MainFrame.BorderSizePixel = 0
    MainFrame.ClipsDescendants = true
    MainFrame.Parent = ScreenGui

    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 10)
    MainCorner.Parent = MainFrame

    local MainStroke = Instance.new("UIStroke")
    MainStroke.Color = AccentColor
    MainStroke.Thickness = 2
    MainStroke.Parent = MainFrame

    MakeDraggable(MainFrame)

    -- الشريط العلوي (Top Bar)
    local TopBar = Instance.new("Frame")
    TopBar.Size = UDim2.new(1, 0, 0, 40)
    TopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    TopBar.BorderSizePixel = 0
    TopBar.Parent = MainFrame

    local TopTitle = Instance.new("TextLabel")
    TopTitle.Text = Title
    TopTitle.Size = UDim2.new(0.6, 0, 1, 0)
    TopTitle.Position = UDim2.new(0, 15, 0, 0)
    TopTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
    TopTitle.TextXAlignment = Enum.TextXAlignment.Left
    TopTitle.Font = Enum.Font.GothamBold
    TopTitle.TextSize = 14
    TopTitle.BackgroundTransparency = 1
    TopTitle.Parent = TopBar

    -- أزرار الإغلاق والتصغير في الأعلى
    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Size = UDim2.new(0, 30, 0, 30)
    CloseBtn.Position = UDim2.new(1, -35, 0, 5)
    CloseBtn.Text = "✕"
    CloseBtn.TextColor3 = AccentColor
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.TextSize = 14
    CloseBtn.BackgroundTransparency = 1
    CloseBtn.Parent = TopBar

    local MinBtn = Instance.new("TextButton")
    MinBtn.Size = UDim2.new(0, 30, 0, 30)
    MinBtn.Position = UDim2.new(1, -65, 0, 5)
    MinBtn.Text = "—"
    MinBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    MinBtn.Font = Enum.Font.GothamBold
    MinBtn.TextSize = 14
    MinBtn.BackgroundTransparency = 1
    MinBtn.Parent = TopBar

    -- شريط الـ Tabs الجانبي (على اليسار)
    local TabBar = Instance.new("ScrollingFrame")
    TabBar.Size = UDim2.new(0, 140, 1, -40)
    TabBar.Position = UDim2.new(0, 0, 0, 40)
    TabBar.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    TabBar.BorderSizePixel = 0
    TabBar.ScrollBarThickness = 2
    TabBar.Parent = MainFrame

    local TabListLayout = Instance.new("UIListLayout")
    TabListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    TabListLayout.Padding = UDim.new(0, 5)
    TabListLayout.Parent = TabBar

    -- حاوية المحتوى (Container)
    local ContentContainer = Instance.new("Frame")
    ContentContainer.Size = UDim2.new(1, -145, 1, -45)
    ContentContainer.Position = UDim2.new(0, 145, 0, 42)
    ContentContainer.BackgroundTransparency = 1
    ContentContainer.Parent = MainFrame

    -- التحكم بالتصغير والإغلاق (Animations)
    local isOpen = true
    local function ToggleGUI()
        isOpen = not isOpen
        if isOpen then
            MainFrame.Visible = true
            TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart), {Size = UDim2.new(0, 550, 0, 350)}):Play()
        else
            local tween = TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart), {Size = UDim2.new(0, 550, 0, 0)})
            tween:Play()
            tween.Completed:Connect(function()
                if not isOpen then MainFrame.Visible = false end
            end)
        end
    end

    MinBtn.MouseButton1Click:Connect(ToggleGUI)
    OpenBtn.MouseButton1Click:Connect(ToggleGUI)

    CloseBtn.MouseButton1Click:Connect(function()
        ScreenGui:Destroy()
    end)

    local Window = {}
    local FirstTab = true

    -- إنشاء Tab جديد
    function Window:CreateTab(name, iconUrl)
        local TabBtn = Instance.new("TextButton")
        TabBtn.Size = UDim2.new(0.9, 0, 0, 35)
        TabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        TabBtn.Text = "   " .. name
        TabBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
        TabBtn.Font = Enum.Font.Gotham
        TabBtn.TextSize = 12
        TabBtn.TextXAlignment = Enum.TextXAlignment.Left
        TabBtn.Parent = TabBar

        local TabCorner = Instance.new("UICorner")
        TabCorner.CornerRadius = UDim.new(0, 6)
        TabCorner.Parent = TabBtn

        if iconUrl and iconUrl ~= "" then
            local Icon = Instance.new("ImageLabel")
            Icon.Size = UDim2.new(0, 18, 0, 18)
            Icon.Position = UDim2.new(0, 8, 0.5, -9)
            Icon.BackgroundTransparency = 1
            Icon.Image = GetImage(iconUrl)
            Icon.Parent = TabBtn
            TabBtn.Text = "       " .. name
        end

        local TabPage = Instance.new("ScrollingFrame")
        TabPage.Size = UDim2.new(1, 0, 1, 0)
        TabPage.BackgroundTransparency = 1
        TabPage.Visible = false
        TabPage.ScrollBarThickness = 3
        TabPage.Parent = ContentContainer

        local PageLayout = Instance.new("UIListLayout")
        PageLayout.Padding = UDim.new(0, 8)
        PageLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        PageLayout.Parent = TabPage

        if FirstTab then
            FirstTab = false
            TabPage.Visible = true
            TabBtn.BackgroundColor3 = AccentColor
            TabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end

        TabBtn.MouseButton1Click:Connect(function()
            for _, child in pairs(ContentContainer:GetChildren()) do
                if child:IsA("ScrollingFrame") then child.Visible = false end
            end
            for _, btn in pairs(TabBar:GetChildren()) do
                if btn:IsA("TextButton") then
                    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
                    btn.TextColor3 = Color3.fromRGB(180, 180, 180)
                end
            end
            TabPage.Visible = true
            TabBtn.BackgroundColor3 = AccentColor
            TabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end)

        local Elements = {}

        -- إضافة Section (قسم)
        function Elements:AddSection(title)
            local Sec = Instance.new("TextLabel")
            Sec.Size = UDim2.new(0.95, 0, 0, 25)
            Sec.Text = title
            Sec.TextColor3 = AccentColor
            Sec.Font = Enum.Font.GothamBold
            Sec.TextSize = 12
            Sec.TextXAlignment = Enum.TextXAlignment.Left
            Sec.BackgroundTransparency = 1
            Sec.Parent = TabPage
        end

        -- إضافة Button (زر)
        function Elements:AddButton(text, callback)
            callback = callback or function() end
            local Btn = Instance.new("TextButton")
            Btn.Size = UDim2.new(0.95, 0, 0, 32)
            Btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            Btn.Text = text
            Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            Btn.Font = Enum.Font.Gotham
            Btn.TextSize = 12
            Btn.Parent = TabPage

            local BtnCorner = Instance.new("UICorner")
            BtnCorner.CornerRadius = UDim.new(0, 6)
            BtnCorner.Parent = Btn

            Btn.MouseButton1Click:Connect(function()
                TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = AccentColor}):Play()
                task.wait(0.1)
                TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(30, 30, 30)}):Play()
                callback()
            end)
        end

        -- إضافة Toggle (تفعيل/تعطيل)
        function Elements:AddToggle(text, default, callback)
            local state = default or false
            callback = callback or function() end

            local Tog = Instance.new("TextButton")
            Tog.Size = UDim2.new(0.95, 0, 0, 32)
            Tog.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            Tog.Text = "   " .. text
            Tog.TextColor3 = Color3.fromRGB(255, 255, 255)
            Tog.Font = Enum.Font.Gotham
            Tog.TextSize = 12
            Tog.TextXAlignment = Enum.TextXAlignment.Left
            Tog.Parent = TabPage

            local TogCorner = Instance.new("UICorner")
            TogCorner.CornerRadius = UDim.new(0, 6)
            TogCorner.Parent = Tog

            local Box = Instance.new("Frame")
            Box.Size = UDim2.new(0, 20, 0, 20)
            Box.Position = UDim2.new(1, -28, 0.5, -10)
            Box.BackgroundColor3 = state and AccentColor or Color3.fromRGB(50, 50, 50)
            Box.Parent = Tog

            local BoxCorner = Instance.new("UICorner")
            BoxCorner.CornerRadius = UDim.new(0, 4)
            BoxCorner.Parent = Box

            Tog.MouseButton1Click:Connect(function()
                state = not state
                TweenService:Create(Box, TweenInfo.new(0.2), {BackgroundColor3 = state and AccentColor or Color3.fromRGB(50, 50, 50)}):Play()
                callback(state)
            end)
        end

        -- إضافة Slider (سحب باللمس أو الماوس)
        function Elements:AddSlider(text, min, max, default, callback)
            local val = default or min
            callback = callback or function() end

            local Frame = Instance.new("Frame")
            Frame.Size = UDim2.new(0.95, 0, 0, 45)
            Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            Frame.Parent = TabPage

            local FrameCorner = Instance.new("UICorner")
            FrameCorner.CornerRadius = UDim.new(0, 6)
            FrameCorner.Parent = Frame

            local Title = Instance.new("TextLabel")
            Title.Text = "   " .. text .. " : " .. tostring(val)
            Title.Size = UDim2.new(1, 0, 0, 20)
            Title.TextColor3 = Color3.fromRGB(255, 255, 255)
            Title.Font = Enum.Font.Gotham
            Title.TextSize = 11
            Title.TextXAlignment = Enum.TextXAlignment.Left
            Title.BackgroundTransparency = 1
            Title.Parent = Frame

            local Bar = Instance.new("Frame")
            Bar.Size = UDim2.new(0.9, 0, 0, 6)
            Bar.Position = UDim2.new(0.05, 0, 0.7, -3)
            Bar.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
            Bar.Parent = Frame

            local Fill = Instance.new("Frame")
            Fill.Size = UDim2.new((val - min) / (max - min), 0, 1, 0)
            Fill.BackgroundColor3 = AccentColor
            Fill.BorderSizePixel = 0
            Fill.Parent = Bar

            local dragging = false
            local function Update(input)
                local pos = math.clamp((input.Position.X - Bar.AbsolutePosition.X) / Bar.AbsoluteSize.X, 0, 1)
                val = math.floor(min + ((max - min) * pos))
                Fill.Size = UDim2.new(pos, 0, 1, 0)
                Title.Text = "   " .. text .. " : " .. tostring(val)
                callback(val)
            end

            Bar.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true
                    Update(input)
                end
            end)

            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = false
                end
            end)

            UserInputService.InputChanged:Connect(function(input)
                if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    Update(input)
                end
            end)
        end

        -- إضافة Social Copy (نسخ روابط السوشيال ميديا)
        function Elements:AddSocialLink(platform, url)
            local Btn = Instance.new("TextButton")
            Btn.Size = UDim2.new(0.95, 0, 0, 30)
            Btn.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
            Btn.Text = "Copy " .. platform .. " Link"
            Btn.TextColor3 = Color3.fromRGB(200, 200, 255)
            Btn.Font = Enum.Font.GothamBold
            Btn.TextSize = 11
            Btn.Parent = TabPage

            local BtnCorner = Instance.new("UICorner")
            BtnCorner.CornerRadius = UDim.new(0, 6)
            BtnCorner.Parent = Btn

            Btn.MouseButton1Click:Connect(function()
                if setclipboard then
                    setclipboard(url)
                    Btn.Text = "Copied to Clipboard!"
                    task.wait(1.5)
                    Btn.Text = "Copy " .. platform .. " Link"
                end
            end)
        end

        return Elements
    end

    return Window
end

return Library
