local Configs_HUB = {
    Cor_Hub = Color3.fromRGB(10, 10, 10),         -- أسود داكن
    Cor_Options = Color3.fromRGB(20, 20, 20),     -- أسود رمادي للعناصر
    Cor_Stroke = Color3.fromRGB(140, 0, 255),    -- بنفسجي للحواف
    Cor_Text = Color3.fromRGB(255, 255, 255),     -- أبيض للنصوص
    Cor_DarkText = Color3.fromRGB(200, 200, 200), -- أبيض خفيف للنصوص الفرعية
    Corner_Radius = UDim.new(0, 4),
    Text_Font = Enum.Font.FredokaOne
}

local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local function Create(instance, parent, props)
    local new = Instance.new(instance, parent)
    if props then
        for prop, value in pairs(props) do
            new[prop] = value
        end
    end
    return new
end

local function SetProps(instance, props)
    if instance and props then
        for prop, value in pairs(props) do
            instance[prop] = value
        end
    end
    return instance
end

local function Corner(parent, props)
    local new = Create("UICorner", parent)
    new.CornerRadius = Configs_HUB.Corner_Radius
    if props then SetProps(new, props) end
    return new
end

local function Stroke(parent, props)
    local new = Create("UIStroke", parent)
    new.Color = Configs_HUB.Cor_Stroke
    new.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    if props then SetProps(new, props) end
    return new
end

local function CreateTween(instance, prop, value, time, tweenWait)
    local tween = TweenService:Create(instance, TweenInfo.new(time, Enum.EasingStyle.Linear), {[prop] = value})
    tween:Play()
    if tweenWait then tween.Completed:Wait() end
end

local function TextSetColor(instance)
    instance.MouseEnter:Connect(function()
        CreateTween(instance, "TextColor3", Color3.fromRGB(180, 70, 255), 0.3, false)
    end)
    instance.MouseLeave:Connect(function()
        CreateTween(instance, "TextColor3", Configs_HUB.Cor_Text, 0.3, false)
    end)
end

-- إزالة أي نسخة سابقة
if CoreGui:FindFirstChild("YHUB library") then
    CoreGui["YHUB library"]:Destroy()
end

local ScreenGui = Create("ScreenGui", CoreGui, {
    Name = "YHUB library"
})

function DestroyScript()
    ScreenGui:Destroy()
end

local Menu_Notifi = Create("Frame", ScreenGui, {
    Size = UDim2.new(0, 300, 1, 0),
    Position = UDim2.new(1, 0, 0, 0),
    AnchorPoint = Vector2.new(1, 0),
    BackgroundTransparency = 1
})

Create("UIPadding", Menu_Notifi, {
    PaddingLeft = UDim.new(0, 25),
    PaddingTop = UDim.new(0, 25),
    PaddingBottom = UDim.new(0, 50)
})

Create("UIListLayout", Menu_Notifi, {
    Padding = UDim.new(0, 15),
    VerticalAlignment = Enum.VerticalAlignment.Bottom
})

function MakeNotifi(Configs)
    local Title = Configs.Title or "Notification"
    local text = Configs.Text or "Content"
    local timewait = Configs.Time or 5
    
    local Frame1 = Create("Frame", Menu_Notifi, {
        Size = UDim2.new(1, 0, 0, 0),
        BackgroundTransparency = 1,
        AutomaticSize = Enum.AutomaticSize.Y
    })
    
    local Frame2 = Create("Frame", Frame1, {
        Size = UDim2.new(1, 0, 0, 0),
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Position = UDim2.new(1, 0, 0, 0),
        AutomaticSize = Enum.AutomaticSize.Y
    }) Corner(Frame2) Stroke(Frame2)
    
    local TextLabel = Create("TextLabel", Frame2, {
        Size = UDim2.new(1, -30, 0, 25),
        Font = Configs_HUB.Text_Font,
        BackgroundTransparency = 1,
        Text = Title,
        TextSize = 18,
        Position = UDim2.new(0, 10, 0, 5),
        TextXAlignment = Enum.TextXAlignment.Left,
        TextColor3 = Configs_HUB.Cor_Text
    })
    
    local TextButton = Create("TextButton", Frame2, {
        Text = "X",
        Font = Configs_HUB.Text_Font,
        TextSize = 18,
        BackgroundTransparency = 1,
        TextColor3 = Color3.fromRGB(200, 0, 0),
        Position = UDim2.new(1, -5, 0, 5),
        AnchorPoint = Vector2.new(1, 0),
        Size = UDim2.new(0, 25, 0, 25)
    })
    
    local TextDesc = Create("TextLabel", Frame2, {
        Size = UDim2.new(1, -20, 0, 0),
        Position = UDim2.new(0, 10, 0, 30),
        TextSize = 14,
        TextColor3 = Configs_HUB.Cor_DarkText,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        AutomaticSize = Enum.AutomaticSize.Y,
        Text = text,
        Font = Configs_HUB.Text_Font,
        BackgroundTransparency = 1,
        TextWrapped = true
    })
    
    local FrameSize = Create("Frame", Frame2, {
        Size = UDim2.new(1, 0, 0, 2),
        BackgroundColor3 = Configs_HUB.Cor_Stroke,
        Position = UDim2.new(0, 0, 1, -2),
        BorderSizePixel = 0
    })
    
    task.spawn(function()
        CreateTween(FrameSize, "Size", UDim2.new(0, 0, 0, 2), timewait, true)
    end)
    
    TextButton.MouseButton1Click:Connect(function()
        CreateTween(Frame2, "Position", UDim2.new(1, 200, 0, 0), 0.3, true)
        Frame1:Destroy()
    end)
    
    task.spawn(function()
        CreateTween(Frame2, "Position", UDim2.new(0, 0, 0, 0), 0.3, true)
        task.wait(timewait)
        if Frame2 then
            CreateTween(Frame2, "Position", UDim2.new(1, 200, 0, 0), 0.3, true)
            Frame1:Destroy()
        end
    end)
end

function MakeWindow(Configs)
    local title = Configs.Hub.Title or "REDz HUB"
    local Anim_Title = Configs.Hub.Animation or "by : redz9999"
    local HubBackgroundImage = Configs.Hub.BackgroundImage or ""
    
    local KeySystem = Configs.Key and Configs.Key.KeySystem or false
    local KeyTitle = Configs.Key and Configs.Key.Title or "Key System"
    local KeyDescription = Configs.Key and Configs.Key.Description or "Enter Key"
    local KeyKey = Configs.Key and Configs.Key.Keys or {"123"}
    local KeyLink = Configs.Key and Configs.Key.KeyLink or ""
    local KeyNotifications = Configs.Key and Configs.Key.Notifi and Configs.Key.Notifi.Notifications or true
    
    if KeySystem then
        local KeyMenu = Create("Frame", ScreenGui, {
            Size = UDim2.new(0, 400, 0, 220),
            Position = UDim2.new(0.5, 0, 0.5, 0),
            BackgroundColor3 = Configs_HUB.Cor_Hub,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Active = true,
            Draggable = true
        }) Corner(KeyMenu) Stroke(KeyMenu)
        
        local CloseButton = Create("TextButton", KeyMenu, {
            Size = UDim2.new(0, 30, 0, 30),
            Position = UDim2.new(1, -10, 0, 5),
            AnchorPoint = Vector2.new(1, 0),
            Text = "X",
            Font = Configs_HUB.Text_Font,
            TextSize = 20,
            TextColor3 = Color3.fromRGB(240, 0, 0),
            BackgroundTransparency = 1,
        })
        
        local TitleKey = Create("TextLabel", KeyMenu, {
            Size = UDim2.new(1, -80, 0, 25),
            Position = UDim2.new(0, 20, 0, 5),
            Text = KeyTitle,
            Font = Configs_HUB.Text_Font,
            TextSize = 20,
            TextColor3 = Configs_HUB.Cor_Text,
            TextXAlignment = Enum.TextXAlignment.Left,
            BackgroundTransparency = 1
        })
        
        local Description = Create("TextLabel", KeyMenu, {
            Size = UDim2.new(1, -40, 0, 40),
            Text = KeyDescription,
            TextSize = 14,
            TextColor3 = Configs_HUB.Cor_DarkText,
            Font = Configs_HUB.Text_Font,
            Position = UDim2.new(0, 20, 0, 30),
            TextXAlignment = Enum.TextXAlignment.Left,
            TextYAlignment = Enum.TextYAlignment.Top,
            BackgroundTransparency = 1,
            TextWrapped = true
        })
        
        local TextBox = Create("TextBox", KeyMenu, {
            Size = UDim2.new(1, -40, 0, 40),
            Position = UDim2.new(0, 20, 0, 80),
            BackgroundColor3 = Configs_HUB.Cor_Options,
            PlaceholderText = "Enter Key Here...",
            Text = "",
            TextColor3 = Configs_HUB.Cor_Text,
            Font = Configs_HUB.Text_Font,
            TextSize = 18
        }) Corner(TextBox) Stroke(TextBox)
        
        local ConfirmButton = Create("TextButton", KeyMenu, {
            Text = "Submit",
            Font = Configs_HUB.Text_Font,
            TextSize = 16,
            TextColor3 = Configs_HUB.Cor_Text,
            Size = UDim2.new(0, 160, 0, 40),
            Position = UDim2.new(1, -20, 0, 140),
            AnchorPoint = Vector2.new(1, 0),
            BackgroundColor3 = Configs_HUB.Cor_Options
        }) Corner(ConfirmButton) Stroke(ConfirmButton)
        
        local GetKeyLink = Create("TextButton", KeyMenu, {
            Text = "Get Key Link",
            Font = Configs_HUB.Text_Font,
            TextSize = 16,
            TextColor3 = Configs_HUB.Cor_Text,
            Size = UDim2.new(0, 160, 0, 40),
            Position = UDim2.new(0, 20, 0, 140),
            BackgroundColor3 = Configs_HUB.Cor_Options
        }) Corner(GetKeyLink) Stroke(GetKeyLink)
        
        local KeyVerify = false
        CloseButton.MouseButton1Click:Connect(function()
            ScreenGui:Destroy()
        end)
        
        ConfirmButton.MouseButton1Click:Connect(function()
            for _, v in pairs(KeyKey) do
                if TextBox.Text == v then
                    KeyVerify = true
                end
            end
            if not KeyVerify then
                MakeNotifi({Title = KeyTitle, Text = "Incorrect Key!", Time = 3})
            end
        end)
        
        GetKeyLink.MouseButton1Click:Connect(function()
            if setclipboard then
                setclipboard(KeyLink)
                MakeNotifi({Title = KeyTitle, Text = "Link copied to clipboard!", Time = 3})
            end
        end)
        
        repeat task.wait() until KeyVerify
        KeyMenu:Destroy()
    end
    
    local Menu = Create("Frame", ScreenGui, {
        BackgroundColor3 = Configs_HUB.Cor_Hub,
        Position = UDim2.new(0.5, -250, 0.5, -135),
        Size = UDim2.new(0, 500, 0, 270),
        Active = true,
        Draggable = true,
        ClipsDescendants = true
    }) Corner(Menu) Stroke(Menu)
    
    -- خلفية الصورة لـ GUI الرئيسي
    local MainBgImage = Create("ImageLabel", Menu, {
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundTransparency = 1,
        Image = HubBackgroundImage,
        ImageTransparency = 0.5,
        ScaleType = Enum.ScaleType.Crop
    })
    
    local TopBar = Create("Frame", Menu, {
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 0, 30),
        ZIndex = 2
    })
    
    local Title = Create("TextLabel", TopBar, {
        Size = UDim2.new(1, -80, 1, 0),
        Position = UDim2.new(0, 15, 0, 0),
        TextColor3 = Configs_HUB.Cor_Text,
        Font = Configs_HUB.Text_Font,
        TextXAlignment = Enum.TextXAlignment.Left,
        Text = title,
        TextSize = 18,
        BackgroundTransparency = 1
    })
    
    local ButtonsFrame = Create("Frame", TopBar, {
        Size = UDim2.new(0, 60, 1, 0),
        Position = UDim2.new(1, -5, 0, 0),
        AnchorPoint = Vector2.new(1, 0),
        BackgroundTransparency = 1
    })
    
    local Minimize_BTN = Create("TextButton", ButtonsFrame, {
        Text = "-",
        TextColor3 = Configs_HUB.Cor_Text,
        Size = UDim2.new(0.5, 0, 1, 0),
        BackgroundTransparency = 1,
        Font = Configs_HUB.Text_Font,
        TextSize = 20
    })
    
    local Close_Button = Create("TextButton", ButtonsFrame, {
        Text = "×",
        TextColor3 = Color3.fromRGB(255, 50, 50),
        Size = UDim2.new(0.5, 0, 1, 0),
        Position = UDim2.new(0.5, 0, 0, 0),
        BackgroundTransparency = 1,
        Font = Configs_HUB.Text_Font,
        TextSize = 22
    })
    
    local IsMinimized = false
    Minimize_BTN.MouseButton1Click:Connect(function()
        IsMinimized = not IsMinimized
        Minimize_BTN.Text = IsMinimized and "+" or "-"
        CreateTween(Menu, "Size", IsMinimized and UDim2.new(0, 500, 0, 30) or UDim2.new(0, 500, 0, 270), 0.2, false)
    end)
    
    Close_Button.MouseButton1Click:Connect(function()
        DestroyScript()
    end)
    
    local ScrollBar = Create("ScrollingFrame", Menu, {
        Size = UDim2.new(0, 130, 1, -35),
        Position = UDim2.new(0, 5, 0, 30),
        CanvasSize = UDim2.new(),
        ScrollingDirection = Enum.ScrollingDirection.Y,
        AutomaticCanvasSize = Enum.AutomaticCanvasSize.Y,
        BackgroundTransparency = 1,
        ScrollBarThickness = 2,
        ZIndex = 2
    })
    
    Create("UIPadding", ScrollBar, {
        PaddingLeft = UDim.new(0, 2),
        PaddingRight = UDim.new(0, 2),
        PaddingTop = UDim.new(0, 5),
        PaddingBottom = UDim.new(0, 5)
    })
    Create("UIListLayout", ScrollBar, { Padding = UDim.new(0, 5) })
    
    local Containers = Create("Frame", Menu, {
        Size = UDim2.new(1, -145, 1, -35),
        Position = UDim2.new(1, -5, 0, 30),
        AnchorPoint = Vector2.new(1, 0),
        BackgroundTransparency = 1,
        ZIndex = 2
    })
    
    local firstVisible = true
    
    local WindowAPI = {}
    
    function WindowAPI:MakeTab(Configs)
        local TabName = Configs.Name or "Tab"
        local TabImage = Configs.Image or "" -- خلفية أو أيقونة للـ Tab
        
        local Frame = Create("Frame", ScrollBar, {
            Size = UDim2.new(1, 0, 0, 28),
            BackgroundColor3 = Configs_HUB.Cor_Options,
            ClipsDescendants = true
        }) Corner(Frame) Stroke(Frame)
        
        -- خلفية صورة للـ Tab Button
        if TabImage ~= "" then
            Create("ImageLabel", Frame, {
                Size = UDim2.new(1, 0, 1, 0),
                Image = TabImage,
                BackgroundTransparency = 1,
                ImageTransparency = 0.6,
                ScaleType = Enum.ScaleType.Crop
            })
        end
        
        local TextButton = Create("TextButton", Frame, {
            Size = UDim2.new(1, 0, 1, 0),
            BackgroundTransparency = 1,
            Text = TabName,
            Font = Configs_HUB.Text_Font,
            TextColor3 = firstVisible and Configs_HUB.Cor_Text or Configs_HUB.Cor_DarkText,
            TextSize = 14,
            ZIndex = 2
        })
        
        local Container = Create("ScrollingFrame", Containers, {
            Size = UDim2.new(1, 0, 1, 0),
            ScrollingDirection = Enum.ScrollingDirection.Y,
            AutomaticCanvasSize = Enum.AutomaticCanvasSize.Y,
            CanvasSize = UDim2.new(),
            BackgroundTransparency = 1,
            ScrollBarThickness = 2,
            Visible = firstVisible
        })
        
        Create("UIPadding", Container, {
            PaddingLeft = UDim.new(0, 5),
            PaddingRight = UDim.new(0, 5),
            PaddingTop = UDim.new(0, 5),
            PaddingBottom = UDim.new(0, 5)
        })
        Create("UIListLayout", Container, { Padding = UDim.new(0, 5) })
        
        TextButton.MouseButton1Click:Connect(function()
            for _, child in pairs(Containers:GetChildren()) do
                if child:IsA("ScrollingFrame") then child.Visible = false end
            end
            for _, child in pairs(ScrollBar:GetChildren()) do
                if child:IsA("Frame") and child:FindFirstChild("TextButton") then
                    child.TextButton.TextColor3 = Configs_HUB.Cor_DarkText
                end
            end
            Container.Visible = true
            TextButton.TextColor3 = Configs_HUB.Cor_Text
        end)
        
        firstVisible = false
        
        local TabAPI = {}
        
        function TabAPI:AddButton(Configs)
            local ButtonName = Configs.Name or "Button"
            local Callback = Configs.Callback or function() end
            
            local TextButton = Create("TextButton", Container, {
                Size = UDim2.new(1, 0, 0, 30),
                BackgroundColor3 = Configs_HUB.Cor_Options,
                Text = ButtonName,
                Font = Configs_HUB.Text_Font,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 14,
                AutoButtonColor = false
            }) Corner(TextButton) Stroke(TextButton)
            
            TextButton.MouseButton1Click:Connect(function()
                Callback()
            end)
            TextSetColor(TextButton)
        end
        
        function TabAPI:AddToggle(Configs)
            local ToggleName = Configs.Name or "Toggle"
            local Default = Configs.Default or false
            local Callback = Configs.Callback or function() end
            
            local TextButton = Create("TextButton", Container, {
                Size = UDim2.new(1, 0, 0, 30),
                BackgroundColor3 = Configs_HUB.Cor_Options,
                Text = "  " .. ToggleName,
                Font = Configs_HUB.Text_Font,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left,
                AutoButtonColor = false
            }) Corner(TextButton) Stroke(TextButton)
            
            local ToggleBox = Create("Frame", TextButton, {
                Size = UDim2.new(0, 35, 0, 18),
                Position = UDim2.new(1, -40, 0.5, 0),
                AnchorPoint = Vector2.new(0, 0.5),
                BackgroundColor3 = Configs_HUB.Cor_Hub
            }) Corner(ToggleBox, {CornerRadius = UDim.new(1, 0)}) Stroke(ToggleBox)
            
            local Circle = Create("Frame", ToggleBox, {
                Size = UDim2.new(0, 14, 0, 14),
                Position = Default and UDim2.new(1, -16, 0.5, 0) or UDim2.new(0, 2, 0.5, 0),
                AnchorPoint = Vector2.new(0, 0.5),
                BackgroundColor3 = Default and Color3.fromRGB(140, 0, 255) or Configs_HUB.Cor_DarkText
            }) Corner(Circle, {CornerRadius = UDim.new(1, 0)})
            
            local Toggled = Default
            Callback(Toggled)
            
            TextButton.MouseButton1Click:Connect(function()
                Toggled = not Toggled
                CreateTween(Circle, "Position", Toggled and UDim2.new(1, -16, 0.5, 0) or UDim2.new(0, 2, 0.5, 0), 0.2, false)
                CreateTween(Circle, "BackgroundColor3", Toggled and Color3.fromRGB(140, 0, 255) or Configs_HUB.Cor_DarkText, 0.2, false)
                Callback(Toggled)
            end)
        end
        
        function TabAPI:AddSlider(Configs)
            local SliderName = Configs.Name or "Slider"
            local Min = Configs.Min or 0
            local Max = Configs.Max or 100
            local Default = Configs.Default or Min
            local Callback = Configs.Callback or function() end
            
            local Frame = Create("Frame", Container, {
                Size = UDim2.new(1, 0, 0, 45),
                BackgroundColor3 = Configs_HUB.Cor_Options
            }) Corner(Frame) Stroke(Frame)
            
            local TextLabel = Create("TextLabel", Frame, {
                Size = UDim2.new(1, -50, 0, 20),
                Position = UDim2.new(0, 10, 0, 2),
                Text = SliderName,
                Font = Configs_HUB.Text_Font,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left,
                BackgroundTransparency = 1
            })
            
            local ValueLabel = Create("TextLabel", Frame, {
                Size = UDim2.new(0, 40, 0, 20),
                Position = UDim2.new(1, -45, 0, 2),
                Text = tostring(Default),
                Font = Configs_HUB.Text_Font,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 14,
                BackgroundTransparency = 1
            })
            
            local Bar = Create("TextButton", Frame, {
                Size = UDim2.new(1, -20, 0, 8),
                Position = UDim2.new(0, 10, 0, 28),
                BackgroundColor3 = Configs_HUB.Cor_Hub,
                Text = ""
            }) Corner(Bar)
            
            local Fill = Create("Frame", Bar, {
                Size = UDim2.new((Default - Min) / (Max - Min), 0, 1, 0),
                BackgroundColor3 = Configs_HUB.Cor_Stroke
            }) Corner(Fill)
            
            local Dragging = false
            local function UpdateSlider()
                local MousePos = UserInputService:GetMouseLocation().X - Bar.AbsolutePosition.X
                local Percentage = math.clamp(MousePos / Bar.AbsoluteSize.X, 0, 1)
                local Value = math.floor(Min + (Max - Min) * Percentage)
                Fill.Size = UDim2.new(Percentage, 0, 1, 0)
                ValueLabel.Text = tostring(Value)
                Callback(Value)
            end
            
            Bar.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    Dragging = true
                    UpdateSlider()
                end
            end)
            
            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    Dragging = false
                end
            end)
            
            UserInputService.InputChanged:Connect(function(input)
                if Dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    UpdateSlider()
                end
            end)
        end

        function TabAPI:AddDropdown(Configs)
            local DropName = Configs.Name or "Dropdown"
            local Options = Configs.Options or {}
            local Callback = Configs.Callback or function() end
            
            local Frame = Create("Frame", Container, {
                Size = UDim2.new(1, 0, 0, 30),
                BackgroundColor3 = Configs_HUB.Cor_Options,
                ClipsDescendants = true
            }) Corner(Frame) Stroke(Frame)
            
            local DropButton = Create("TextButton", Frame, {
                Size = UDim2.new(1, 0, 0, 30),
                BackgroundTransparency = 1,
                Text = "  " .. DropName,
                Font = Configs_HUB.Text_Font,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left
            })
            
            local Arrow = Create("TextLabel", DropButton, {
                Size = UDim2.new(0, 30, 1, 0),
                Position = UDim2.new(1, -30, 0, 0),
                Text = "▼",
                Font = Configs_HUB.Text_Font,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 12,
                BackgroundTransparency = 1
            })
            
            local DropContent = Create("Frame", Frame, {
                Size = UDim2.new(1, -10, 0, 0),
                Position = UDim2.new(0, 5, 0, 30),
                BackgroundTransparency = 1
            }) Create("UIListLayout", DropContent, { Padding = UDim.new(0, 2) })
            
            local Opened = false
            DropButton.MouseButton1Click:Connect(function()
                Opened = not Opened
                Arrow.Text = Opened and "▲" or "▼"
                local TargetSize = Opened and UDim2.new(1, 0, 0, 35 + (#Options * 22)) or UDim2.new(1, 0, 0, 30)
                CreateTween(Frame, "Size", TargetSize, 0.2, false)
            end)
            
            for _, opt in pairs(Options) do
                local OptBtn = Create("TextButton", DropContent, {
                    Size = UDim2.new(1, 0, 0, 20),
                    BackgroundColor3 = Configs_HUB.Cor_Hub,
                    Text = opt,
                    Font = Configs_HUB.Text_Font,
                    TextColor3 = Configs_HUB.Cor_DarkText,
                    TextSize = 12
                }) Corner(OptBtn)
                
                OptBtn.MouseButton1Click:Connect(function()
                    DropButton.Text = "  " .. DropName .. " (" .. opt .. ")"
                    Callback(opt)
                    Opened = false
                    Arrow.Text = "▼"
                    CreateTween(Frame, "Size", UDim2.new(1, 0, 0, 30), 0.2, false)
                end)
            end
        end

        function TabAPI:AddTextBox(Configs)
            local BoxName = Configs.Name or "TextBox"
            local Placeholder = Configs.Placeholder or "Type here..."
            local Callback = Configs.Callback or function() end
            
            local Frame = Create("Frame", Container, {
                Size = UDim2.new(1, 0, 0, 35),
                BackgroundColor3 = Configs_HUB.Cor_Options
            }) Corner(Frame) Stroke(Frame)
            
            local TextLabel = Create("TextLabel", Frame, {
                Size = UDim2.new(0.4, 0, 1, 0),
                Position = UDim2.new(0, 10, 0, 0),
                Text = BoxName,
                Font = Configs_HUB.Text_Font,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 14,
                TextXAlignment = Enum.TextXAlignment.Left,
                BackgroundTransparency = 1
            })
            
            local TextBox = Create("TextBox", Frame, {
                Size = UDim2.new(0.55, -10, 0, 25),
                Position = UDim2.new(0.45, 0, 0.5, 0),
                AnchorPoint = Vector2.new(0, 0.5),
                BackgroundColor3 = Configs_HUB.Cor_Hub,
                PlaceholderText = Placeholder,
                Text = "",
                Font = Configs_HUB.Text_Font,
                TextColor3 = Configs_HUB.Cor_Text,
                TextSize = 12
            }) Corner(TextBox) Stroke(TextBox)
            
            TextBox.FocusLost:Connect(function()
                Callback(TextBox.Text)
            end)
        end

        return TabAPI
    end
    
    return WindowAPI
end
